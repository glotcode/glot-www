(function() {

    $(document).on("click", ".generate", function(e) {
        console.log("click");
        e.preventDefault();
        generate();
        return false;
    });

    function generate() {
        XHR.jsonPut("@{AccountTokenR}", {}).done(function(data) {
          Location.reload();
        }).fail(function(xhr) {
          console.log("failed");
        });
    }
})();
